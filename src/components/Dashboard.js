import React from 'react';

const Dashboard = () => (
  <div>
    <h1>This is the protected admin page</h1>
    <p>It is accessible only if you are logged in!</p>
  </div>
);

export default Dashboard;
